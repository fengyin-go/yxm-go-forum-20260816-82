# 热门主题与浏览数统计问题

## Bug 是什么
主题详情读取、浏览数累加、热门主题排序和热门主题默认数量之间存在组合性错误，
导致查看一次后浏览数增量异常、热门主题按浏览数升序返回、未传 limit 时只返回 1 条。

## 如何触发
启动服务后创建多个主题并分别设置不同浏览数，然后：

```bash
curl 'http://127.0.0.1:8080/api/topics/{id}'
curl 'http://127.0.0.1:8080/api/stats/hot'
```

## 错误信息
查看主题一次后浏览数只增加了 2 而不是 1；热门主题未按浏览数降序排列；不带 limit 参数时
只返回 1 条。对应测试 `TestHotTopicsAndViewCount` 失败。
