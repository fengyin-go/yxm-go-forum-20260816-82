# 版块排序与版块统计问题

## Bug 是什么
版块名称校验、重名检测、版块列表排序和按版块统计的排序之间存在组合性错误，
导致空白名称被接受、重名版块可创建、列表按排序值倒序返回、版块统计按主题数升序返回。

## 如何触发
启动服务后创建若干版块和主题，然后：

```bash
curl -X POST 'http://127.0.0.1:8080/api/boards' -d '{"name":"   ","sort_order":0}'
curl 'http://127.0.0.1:8080/api/boards'
curl 'http://127.0.0.1:8080/api/stats/boards'
```

## 错误信息
空白名称版块被接受；重名版块未返回冲突；版块列表未按排序值升序排列；按版块统计未按
主题数降序排列。对应测试 `TestBoardSortAndStats` 失败。
